node_modules
dist
.ssr
.vite
.DS_Store
.env
.env.local

# Large media — host on CDN, not git
public/**/*.mp4
public/**/*.mov
# public/reel-preview.gif — committed, small enough

# Allow compressed grid videos
!public/grid/*-compressed.mp4

# Grid placeholder images — serve from CDN or Sanity, not git
public/grid/*.png
public/grid/*.jpg

# Allow named compressed grid images
!public/grid/*-compressed.jpg
.vercel

# Real workspace metrics. The repo is public; traffic, leads and search
# figures are not. src/data/metrics.js globs for this and finds nothing on a
# clone, which is the case the dashboard's empty state is for.
src/data/metrics.local.js
